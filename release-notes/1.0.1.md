- Added `One plate two cryosections` project type.
- In `One plate one cryosection`, Design and Collection now use a single 12-column plate header without 6/7 split.
- Added `Not used (N)` sample type and excluded it from downstream coordinate/cut calculations.
- In Collection, column Area inputs are disabled only when the full column is `Not used`.
- Added consistent warning messaging when clicking locked collection columns without Area values.
- Updated macOS About dialog to show LMDmapper version and build date.
- Standardized build artifact naming to `lmdmapper_version_arch_os.ext`.
